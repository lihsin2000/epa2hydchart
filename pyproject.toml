[project]
name = "epa2hydchart"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyinstaller>=6.20.0",
    "setuptools>=82.0.1",
]
